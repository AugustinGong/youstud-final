<h1>Tutti i miei libri</h1>

<div class="row">
    <div class="col-12">
        <% @book.all.each do |book| %>
            <%= image_tag book.image.url, class: "img img-fluid" %>
        <% end %>
    </div>
</div>